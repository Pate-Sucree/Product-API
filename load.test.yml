config:
  target: "http://localhost:4000"
  phases:
    - duration: 30
      arrivalRate: 1000
      name: Sustained load
scenarios:
  # We define one scenario:

  - name: "Product Style by Id"
    flow:
      - get:
          url: "/products/3/styles"

